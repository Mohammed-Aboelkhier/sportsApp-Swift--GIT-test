# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SportsApp(Swift Project)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SportsApp(Swift Project)
	pod 'Alamofire', '~> 5.0.0-rc.2'
	pod 'Kingfisher', '~> 7.0'

  target 'SportsApp(Swift Project)Tests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SportsApp(Swift Project)UITests' do
    # Pods for testing
  end

end
